name: sicsa_mobile
version: 1.0.0+1
description: Aplicación móvil SICSA para pacientes.

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.4.0
  http: ^1.2.2
  flutter_secure_storage: ^9.0.0
  go_router: ^13.2.0
  provider: ^6.1.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true